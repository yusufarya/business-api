export class TransactionHelper {
    static stock_SA (type: string, noTransaction: string, product_id: number, qty: number) {
        if(type == 'in') {

        } else if(type == 'out') {

        }
    }
}